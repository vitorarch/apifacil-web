const container = " flex box-border justify-center mt-[20vh]";
const card = " p-8 w-[600px] flex flex-col items-center";
const header = " w-full text-center mb-5";
const limit = " w-[350px]"
const passwords = " w-full flex flex-col items-center gap-6 mt-5";

const passwordResetCss = {
  container,
  card,
  header,
  limit,
  passwords
}

export default passwordResetCss;
